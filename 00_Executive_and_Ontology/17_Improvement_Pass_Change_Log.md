# Improvement Pass Change Log

## Local definitions

Corpus Arbitration Layer [CAL] means the component that answers unstructured questions against LMS-Graph.

Minimum Viable Product [MVP] means the smallest product that proves the core AGE loop.

## Main improvement

This pass turns the archive from a set of polished subsystem notes into a stronger development handoff package.

## Changes made

- Rewrote the executive brief for directness, readability, and risk/reward balance.
- Rewrote the ontology primer to distinguish Anchored Corpus Arbitration, CAL, Rules Service, Role Service, and Agent Service.
- Rebuilt the claims register around testable claims and claims not made.
- Added the MVP scope charter.
- Added the human authority model.
- Added the output envelope summary.
- Added the engineering handoff directory.
- Rewrote all subsystem white papers into a uniform, easier-to-brief structure.
- Added TickPolicy, overlay composition, CAL ruling mode, Rules Service test corpus, and benchmark matrix files.
- Removed a compatibility-only duplicate white paper file that added terminology noise.

## Result

The archive now provides executive clarity, subsystem definition, implementation order, interface contract names, acceptance tests, and risk discipline while preserving the guidance-pass ontology.
